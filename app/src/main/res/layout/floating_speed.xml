<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/floatingContainer"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/floating_background"
    android:padding="12dp">

    <!-- Close button (top-right) -->
    <TextView
        android:id="@+id/closeButton"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="-4dp"
        android:layout_marginEnd="-4dp"
        android:text="✕"
        android:textColor="#AAAAAA"
        android:textSize="18sp"
        android:gravity="center"
        android:background="@android:color/transparent" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginTop="8dp">

        <!-- Horizontal layout: Speed/Limit (default for free users) -->
        <LinearLayout
            android:id="@+id/horizontalSpeedContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Current Speed -->
            <TextView
                android:id="@+id/floatingSpeedText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0"
                android:textColor="@android:color/white"
                android:textSize="56sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-condensed"
                android:includeFontPadding="false" />

            <!-- Separator -->
            <TextView
                android:id="@+id/floatingSeparator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="/"
                android:textColor="#888888"
                android:textSize="40sp"
                android:fontFamily="sans-serif-condensed"
                android:includeFontPadding="false" />

            <!-- Speed Limit -->
            <TextView
                android:id="@+id/floatingLimitText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="?"
                android:textColor="#888888"
                android:textSize="40sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-condensed"
                android:includeFontPadding="false" />

        </LinearLayout>

        <!-- Vertical layout: Speed over Limit (premium option) -->
        <LinearLayout
            android:id="@+id/verticalSpeedContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone">

            <!-- Current Speed -->
            <TextView
                android:id="@+id/floatingSpeedTextVertical"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0"
                android:textColor="@android:color/white"
                android:textSize="48sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-condensed"
                android:includeFontPadding="false" />

            <!-- Divider line -->
            <View
                android:id="@+id/floatingDivider"
                android:layout_width="60dp"
                android:layout_height="2dp"
                android:background="#888888"
                android:layout_marginVertical="2dp" />

            <!-- Speed Limit -->
            <TextView
                android:id="@+id/floatingLimitTextVertical"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="?"
                android:textColor="#888888"
                android:textSize="36sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-condensed"
                android:includeFontPadding="false" />

        </LinearLayout>

        <!-- Unit label -->
        <TextView
            android:id="@+id/floatingUnitText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="mph"
            android:textColor="#666666"
            android:textSize="12sp"
            android:layout_marginTop="2dp" />

    </LinearLayout>

</FrameLayout>

