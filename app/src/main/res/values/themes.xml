<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main App Theme - Minimalist Black -->
    <style name="Theme.SpeedLimit" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/anim_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.SpeedLimit</item>
    </style>
    
    <!-- Dialog Theme - Dark matching app -->
    <style name="Theme.SpeedLimit.Dialog" parent="Theme.Material3.Dark.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="colorSurface">#1A1A1A</item>
    </style>
</resources>
